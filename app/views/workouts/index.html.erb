<p id="notice"><%= notice %></p>

<h1>Workout</h1>

<table>
  <thead>
    <tr>
      <th>Duration</th>
      <th>Notes</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @workouts.each do |workout| %>
      <tr>
        <td><%= workout.duration %></td>
        <td><%= workout.notes %></td>
        <td><%= link_to 'Show', activity_workout %></td>
        <td><%= link_to 'Edit', edit_activity_workout_path(workout) %></td>
        <td><%= link_to 'Destroy', workout, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Workout', new_workout_path %>
